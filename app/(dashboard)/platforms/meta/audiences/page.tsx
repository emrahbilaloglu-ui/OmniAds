import { ComingSoonState } from "@/components/states/ComingSoonState";

export default function MetaAudiencesPage() {
  return (
    <ComingSoonState
      platformId="meta"
      title="Meta Audiences are coming soon"
      description="Audience intelligence is wired into the platform shell and will ship as a dedicated Meta surface later."
    />
  );
}
