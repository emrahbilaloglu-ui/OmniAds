# Zero-Base Execution Ledger (working copy of master-plan Appendix C)

Source of truth for the template: `ADSECUTE_ZERO_BASE_APPLICATION_IMPLEMENTATION_MASTER_PLAN_2026-08-10.md` (SHA-256 `79b4b4f88b5b89ca06dd52cfaff28c8b21e17d0cde58902fed10594d307ab613`). That file is immutable; this is the working ledger.

## Baseline record (WP-00 attempt, 2026-08-11)

| Field | Value |
|---|---|
| Design ZIP SHA-256 | `0695ae452469ba3efe2615efe3ffd30fcdb88f5847db53d569042fb864c09b9d` |
| Master plan SHA-256 | `79b4b4f88b5b89ca06dd52cfaff28c8b21e17d0cde58902fed10594d307ab613` (verified) |
| `origin/main` | `23e9fc86e95298da71d4afb28567cd300c61e943` |
| Integration branch | `codex/adsecute-zero-base-implementation` |
| Worktree HEAD | `23e9fc86e` (clean) |
| Safety branch | `codex/native-ad-bounded-stop-loss-authority` @ `c46d91c2a` |
| Safety commits in `origin/main` | `31950b1a9` NO · `1517674c7` NO · `e41691f33` NO · `c46d91c2a` NO |
| Node / npm | **node v24.4.1 · npm 11.4.2** (`npm ci`, 705 packages; `package-lock.json` unmodified) |
| Post-G0 HEAD | `7d8bd86c1` · 7 commits ahead of `origin/main` (4 safety commits brought in by the merge + WP-00, WP-00.5, docs) |
| Locale required for PostgreSQL gates | `LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8` |

## Ledger

| WP | Status | Commit | Files/PR | Tests | Visual/AT evidence | Rollback verified | Exceptions |
|---|---|---|---|---|---|---|---|
| 00 | **complete** | `b082885be` | 54 hunks / 29 files | typecheck 0 · lint 0 · vitest 7608/0 · migrations-from-zero PASS · creative:v2:safety PASS · frozen 22/22 | — | `git revert -m 1 b082885be` | G0 ancestry verified for origin/main + all four safety commits |
| 00.5 | **complete** | `59fd7118b` | WP00_5_BASELINE_INVENTORY.md | read-only inventory at G0 | — | revert doc commit | all 5 APIs + notifications PRESENT; workflow/instrumentation tables PRESENT; /api/db-test absent |
| 01 | **complete** | `b36079f65` | 15 vendored contract/spec files + `SOURCE.md`, `ACCEPTED_RESIDUALS.md`, `scripts/zero-base/verify-design-contract.ts` | `zero-base:contract:verify` 23/23 checks PASS · typecheck 0 · lint 0 | — | `git revert b36079f65` | design package remains **NOT READY**: REQ-27, REQ-28 (`M11`), REQ-41 recorded as accepted residuals, never laundered green |
| 02 | **complete** | `29715accb` | `scripts/zero-base/{generate,verify}-contracts.ts`, `lib/zero-base/{generated-contracts,route-registry,control-registry,report-source-registry,contract}.ts` | `test:zero-base:contract` 17/17 · `zero-base:contracts:check` current · typecheck 0 · lint 0 | — | `git revert 29715accb` | gates proven fail-closed by tamper test: leaf-id edit → check stale + 3 test failures; `audit.json` `NOT READY`→`READY` → hash-drift + verdict failure |
| 03 | **complete** | `e2c2d0885` | `lib/access/{authorize-business,require-business-page-context}.ts`, `lib/access-membership.ts`, `lib/workspace/{workspace-context,switch-destination,agency-return}.ts`, `lib/zero-base/rollout.ts`, `lib/access.ts`, `lib/business-context.ts` + 7 test files | 91 new tests · full suite 7699/0 · typecheck 0 · lint 0 | — | `git revert e2c2d0885` | page helper and workspace modules intentionally unused until WP-06; no API gated on rollout or plan |
| 03A | **complete** | `9a98dc176` | `lib/reports/share-fail-closed.ts`, `app/api/reports/[reportId]/share/route.ts`, `app/share/report/[token]/page.tsx` + 2 test files | 15 tests across both flag branches · full suite 7714/0 · migrations-from-zero PASS · creative:v2:safety PASS · frozen 22/22 | — | set/leave `ZERO_BASE_REPORT_SHARE_FAIL_CLOSED=false`; no data rollback | **flag defaults false and is set in no environment file.** Deploying flag-on needs written operator authority plus a decision on already-issued tokens; that authority does not exist, so flag-on was never enabled outside per-test env vars |
| 04 | **complete** | `a1727c835` | `public/fonts/zero-base/*` (2 woff2 + 2 OFL + LICENSES.md), `lib/design/{ledger-tokens,fonts}.ts`, `lib/theme.ts`, `components/theme/theme-control.tsx`, `scripts/zero-base/verify-fonts.ts`, `app/{layout.tsx,globals.css}`, `store/preferences-store.ts` | 26 token/theme unit tests · 5 Playwright first-paint scenarios · fonts:verify PASS · full suite 7742/0 · production build PASS | first paint measured at 1280×720 in light+dark for 5 cookie/OS combinations; build self-hosts both binaries at their exact SHA-256 | `git revert a1727c835`; removing the canonical CSS block restores legacy exactly | Schibsted ships as ONE variable file (400–900), not the archive's 4 identical copies. Tokens named `--ledger-*` because 25 legacy `--adc-*` variables already exist. 6 dark tokens marked DERIVED — absent from the design's dark table |
| 05 | **complete** | `dd34e444f` (05A) · `37f58cd10` (05B/C) | `components/zero-base/{portal,primitives,states,charts,collections}/**`, `component-map.ts`, `lib/zero-base/state-types.ts`, dev deps + lockfile | 51 component tests (21 + 13 + 17) · full suite 7793/0 · typecheck 0 · lint 0 | overlays asserted inside the canonical portal host; keyboard/focus-trap/focus-return exercised in jsdom | `git revert 37f58cd10 dd34e444f`; primitives are unused by legacy pages | No Storybook. Combobox does NOT use Radix Popover (it steals focus from the input); overlays use an explicit focus-return because Radix's implicit restore does not fire for externally-controlled `open`. All 22 vendored components mapped, both directions |
| 06 | **complete** | `13eecae3a` | `components/zero-base/shell/**` (9), `components/workspace/workspace-context-provider.tsx`, `lib/zero-base/navigation.ts`, `app/{a,c/[businessId],ops}/layout.tsx`, `scripts/zero-base/build-shell-harness.tsx`, `components/layout/nav-items.ts` | 20 shell unit tests · 11 Playwright layout checks · full suite 7813/0 | 1440/1280/768/390/320 × light+dark: no page overflow, rail↔drawer swap at 768, context bar present at every width, theme verified applied; B02 footer inside a 640px artboard | `git revert 13eecae3a`; rollout `off` makes the routes 404 and legacy is untouched | Harness found 2 real bugs: rail was 233px (content-box border) and the rail footer sat 732px below a 640px viewport (`min-height:100vh` left the flex parent indefinite). TikTok/Pinterest/Snapchat removed from `platformOrder` only — type+registry retained for legacy routes. `/api/db-test` absent ⇒ no-op; dev playground already fails closed |
| 07 | **complete** | `6d6cba61e` · `cc5a29050` | `lib/zero-base/{instrumentation-schema,instrumentation-contract,instrumentation-sink,instrumentation-client}.ts`, generator + `generated-contracts.ts`, `lib/migrations.ts`, `app/api/instrumentation/event/route.ts` | 33 contract/client tests · migrations-from-zero PASS · instrumentation seam PASS · full suite 7846/0 | — | `git revert cc5a29050 6d6cba61e`; v2 columns are nullable so v1 rows and emitters are unaffected | One table, additive only; vocabularies widened with v1 values restated in full. Seam caught `account_id` as unconstrained TEXT — now pattern-bounded so a URL cannot land in telemetry. 74 emitters ↔ 74 leaves checked both directions; exactly 15 anonymous = the 15 Public leaves |
| 08 | **complete** | `d94e440dc` | `lib/zero-base/search-adapter.ts`, `components/zero-base/search/search-overlay.tsx`, `app/api/search/route.ts` | 19 tests · full suite 7865/0 | — | `git revert d94e440dc`; legacy response shape unchanged | Adapts the existing route behind `contract=zero-base.v1`; cap 50 + X-of-Y; canonical `/c/` deep links only; label exactly `Businesses + Meta entities`; no live Meta |
| 09 | **complete** | `7865057c1` (routing + `/me`) → `45276d383` (composition + Flow K) | `lib/zero-base/{auth-routing,auth-states}.ts`, `components/zero-base/{rollout-provider,auth/auth-states}.tsx`, `components/auth/auth-surface.tsx`, `app/{login,forgot-password,invite/[token],select-language,(dashboard)/select-business}`, `components/shopify/shopify-connect-client-page.tsx`, `app/me/**` | 57 tests (17 routing · 22 states · 18 Flow K) · full suite 7940/0 | rollout ON/OFF frames asserted in jsdom; canonical auth screens share the shell's responsive harness | `git revert 45276d383 7865057c1`; rollout OFF emits the legacy frame byte-for-byte | Auth endpoints unchanged. Rollout reaches client screens via a server-seeded provider, never `NEXT_PUBLIC_*`. `/select-language` now states it is not a selector |
| 10 | **complete** | `15de2901c` (projection) → `45a9ff1ab` (routes + Flow A) → `601094543` · `0a50fa6a8` (**server pagination correction**) → `aaadbdd58` (**cursor provenance + fail-closed correction**) | `lib/zero-base/{agency-projection,agency-directory-store}.ts`, `app/api/agency-today/route.ts` (`contract=zero-base.v1` branch), `components/zero-base/agency/**`, `app/a/desk/{page,clients,withheld}`, `scripts/ephemeral-postgres-agency-directory-seam-child.ts` | 18 projection · 25 view · 12 boundary · **real-PostgreSQL seam over 121 clients** · full suite 8008/0 | 50-client visible scan at 1440/390 both themes; seam proves 121-client keyset paging at 3 page sizes | `git revert aaadbdd58 0a50fa6a8 601094543 45a9ff1ab 15de2901c`; rollout OFF 404s the boundary before any read | **`45a9ff1ab` shipped client pagination behind an unbounded server read and the ledger wrongly called it server-paginated.** `601094543` replaces it with SQL keyset pagination (C collation, name+id total order); the superseded reader is deleted. Legacy projection retained until WP-27 |
| 11 | **complete** | `33d16dd5f` | `lib/zero-base/home/{metric-contract,home-server}.ts`, `components/zero-base/home/**` (4), `app/c/[businessId]/home/page.tsx` + 2 test files, harness + Playwright | 38 tests (19 contract · 19 view) · full suite 8046/0 · typecheck 0 · lint 0 | H03/H04/H08 at 1440/390/320 × light+dark: no page overflow, every chart carries a table alternative, partial + hard banners both visible | `git revert 33d16dd5f`; rollout OFF 404s the route before any read | Comparisons route through `resolveComparison`, **not** the legacy `changePct ?? 0` at `lib/overview-summary-support.ts:136` — that coercion is what made a null comparison print as a 0% flat. Spend is the only metric in `NEUTRAL_BY_POLICY`; spend/CPA/refunds increases are never drawn as good. Reuses `getOverviewData` in-process; no HTTP self-fetch. No resolver file touched |
| 12 | **complete** | `ce8e8c389` | `lib/zero-base/meta/{decisions-url-state,decisions-presentation}.ts`, `components/zero-base/meta/decisions/**` (2), `app/c/[businessId]/meta/decisions/page.tsx` + 2 test files, harness + Playwright | 39 tests (24 module · 15 view) · full suite 8085/0 | 1440/1280/768/390/320 × light+dark; evidence window and snapshot time rendered as visibly distinct fields; hard + partial banners shown together | `git revert ce8e8c389`; `/api/meta/decisions-workspace` is read unchanged | **Served text is the authority**: verdict and metric strings are asserted equal to the exact served bytes, so the surface cannot disagree with the resolver. Client action-math count is zero — held, reviewer and demo rows all resolve to 0 actions. No Ads Manager link is ever labelled as an executed action |
| 13 | **complete** | `d3a3e10e9` → `ada7a6bf4` (**acceptance-review correction**) | `lib/zero-base/meta/{workflow-schema,workflow-view-model}.ts`, `lib/decision-workflow-store.ts`, `app/api/meta/decision-workflow/route.ts`, `components/zero-base/meta/decisions/workflow-overlay.tsx` (mounted in row + inspector), `lib/migrations.ts`, `scripts/ephemeral-postgres-{migrations-check,workflow-overlay-seam-child}.ts` + 4 test files | 13 overlay · 26 view-model · 22 UI · 21 route · **real-PostgreSQL seam** · full suite 8314/0 · migrations-from-zero PASS | 409 conflict, re-apply, dialog focus trap and focus return, and live announcements all exercised with real user events | `git revert ada7a6bf4 d3a3e10e9`; the migration is additive and nullable | **`d3a3e10e9` was backend-only and the ledger wrongly implied a completed WP: there was no chip, menu, dialog, event history, conflict UI or re-apply anywhere.** `ada7a6bf4` builds them. Batched contract mode on the SAME GET (≤200 keys, refused not truncated) so a served page costs one request, not one per row; legacy single-key mode asserted byte-identical. Journal projection excludes `comment` — proven at the DB. A failed overlay read renders "Workflow unknown", never the default "Open". Verdict bytes and row order asserted identical before/after a transition. Seam also caught the latent `$1`/`$1::uuid` bind collision |
| 14 | **complete** | `4486eaaa4` → `5bbfc2ca7` → `7889d4812` (**two acceptance-review corrections**) | `lib/zero-base/meta/{mutation-ceremony,decision-bound-target,dispatch-contract}.ts`, `app/api/meta/decision-action/preflight/route.ts` (third mode + dispatch descriptor), `components/zero-base/meta/decisions/{mutation-ceremony-panel,decisions-client}.tsx`, `scripts/ephemeral-postgres-decision-bound-target-seam-child.ts` + 4 test files | 30 ceremony · 38 component · 34 route · **38 integration against the real handlers** · **real-PostgreSQL seam** · full suite 8369/0 · creative:v2:safety PASS · frozen 22/22 | collect→validate→confirm→re-check→dispatch driven with real user events; every dispatch body posted to the actual route modules | `git revert 7889d4812 5bbfc2ca7 4486eaaa4`; the flag is set in no environment file, so the UI is unreachable either way | **First failure: `4486eaaa4` was a pure model with no mounted UI and a preflight that was not decision-bound.** `5bbfc2ca7` fixed that. **Second failure: `5bbfc2ca7` shipped a production-contract defect — it mapped ad-set bid to `/api/meta/adsets/[adsetId]/bid`, a route that does not exist (`apply-bid` does), and posted `{businessId, mutationId}` to every endpoint, which all four handlers refuse. It survived because the tests mocked a generic dispatch function.** `7889d4812` builds `dispatch-contract.ts` from what the handlers actually demand and proves it by driving the real route modules: campaign/ad-set need the manual origin, confirmation and server-presented account; apply-bid adds a positive-integer `bidAmountMinor` and refuses the legacy units; **ad routes need the manual origin too plus the exact `adId`/`creativeId`** — the integration test caught that omission with `action_origin_required`. The preflight now issues a concrete path and exact body; unprovable actions are **withheld with a reason** rather than rendered as buttons that can only fail; operator fields are validated before the confirmation; and a **fresh preflight runs at dispatch time**, sending the new descriptor's body, not the stale one. No opaque token: resolving one would need a new authoritative mutation endpoint, which is the parallel API this WP forbids — the handler's own re-resolution is the revalidation |
| 15 | **complete** | `55b442178` → `8c9e13eb8` (**acceptance-review correction**) | `lib/zero-base/meta/{automation-posture,intelligence-server,history-adapter}.ts`, `components/zero-base/meta/{intelligence,history,automation}/**` (4), `app/c/[businessId]/meta/{intelligence,history,automation}/page.tsx` + 4 test files, harness + Playwright | 22 posture · 37 Flow I · 12 intelligence · 13 history adapter · full suite 8314/0 | engage, release, read-back agree/disagree/unavailable and refused-write all driven with real user events over stubbed fetch; static harness covers the mirror provider case, real actor/replay rows and partial/degraded sources at 1440/390/320 × light+dark (54 pages, 60/60) | `git revert 8c9e13eb8 55b442178` | **`55b442178` shipped three defects the ledger did not name: History rendered `rows={[]}` behind a "later slice" comment; Intelligence read one boolean and drew one row; AutomationView got no `onEngage`, so confirming changed nothing; and `buildProviderPostures` hard-coded Google to `serving`, printing "Serving" for a provider nobody queried.** `8c9e13eb8` replaces all four with real authorities: the journal (account resolved from this business's assignments, never from the request; actor in three distinct forms; cap disclosed, never a total), nine sources via `Promise.allSettled` (a failed read is `degraded` with its own words, never `unavailable`; a missing collection is "Not reported", never 0), a real POST followed by an **independent** GET read-back (disagreement and unreadable both → unknown), and a genuine Google read (`unknown` when unreadable, `basis: connection_only`) |
| 16 | **complete** | `10b1d35d3` | `lib/zero-base/creative/{engine-posture,performance-adapter,detail-adapter,route-scope}.ts`, `components/zero-base/creative/**` (5), `app/c/[businessId]/creative/{performance,[creativeId]}/page.tsx` + 2 test files | 49 tests (36 adapter · 13 component) · full suite 8418/0 | all five postures asserted on the surface; DOM carries every metric at any viewport, so a narrow width cannot silently drop one | `git revert 10b1d35d3`; additive routes only | Data logic extracted from `StudioOsView`; the 3,800-line component is never mounted. Postures derived from served flags — **shadow is checked before visibility**, since a visible shadow decision is still one nobody stands behind. Band comes from posture + served label, never from a metric (that is `buyerAction` under another name). Missing metric ≠ 0; legacy null filter preserved **and counted**; detail refuses a cross-account deep link with its own reason |
| 17 | **complete** | `9f630c857` | `lib/zero-base/creative/{studio-adapters,share-acknowledgement}.ts`, `components/zero-base/creative/{studio-views,studio-clients,share-media}.tsx`, 5 `app/c/[businessId]/creative/*` routes, `app/api/creatives/share/route.ts` + 3 test files | 43 tests (19 adapter · 18 component · **6 driving the real share route**) · full suite 8461/0 | share ledger rotate/revoke exercised with real user events; public video controls, captions and retry asserted | `git revert 9f630c857`; the acknowledgement gates creation only, so it can be relaxed without deleting any existing share | **Buyer shares now require an explicit financial acknowledgement; POST without it is 400**, checked on the exact value. This closed a real hole: a caller omitting `audience` defaulted to buyer, so a legacy caller could ship money outward with no warning — that existing test now sends the acknowledgement and still asserts the default. No brief delete anywhere; caps print 250/100 only when served, else `Backend cap not supplied`; expired/revoked/missing are one public message and no workspace identity appears |
| 18 | **complete** | `50c27518b` | `lib/zero-base/launchpad/launchpad-contract.ts`, `components/zero-base/launchpad/**` (2), `app/c/[businessId]/meta/launchpad/page.tsx` + 2 test files | 34 tests (22 contract · 12 view) · full suite 8495/0 | disabled reasons, validation focus move and per-item bulk outcomes driven with real user events | `git revert 50c27518b`; canonical route only | **Zero call sites** to launch/add-to-existing in the shipped bundle, proven by a scan that guards itself (fires on the forbidden shape, not on `/meta/templates`, and asserts it scanned a non-empty set); comments stripped so a doc comment explaining the absence is not punished. Disabled Launch names its exact prerequisites; the panel states there is no launch rollback and no Google Launchpad. Templates have no edit, matching an API with no update. Bulk is flag-gated, caps at 20, rejects 21 in the form, and an unmentioned item is `unknown`, never applied |
| 19 | **complete** | `87be1e605` | `lib/zero-base/google/google-contract.ts`, `components/zero-base/google/{google-views,google-clients}.tsx`, 5 `app/c/[businessId]/google/*` routes + 2 test files | 29 tests (17 contract · 12 view) · full suite 8524/0 | scope, source states and reference-card completeness asserted on the rendered surface | `git revert 87be1e605`; canonical routes only | Scope stated everywhere; a portfolio spanning currencies or time zones is **not summed** and says why. Overview/Pulse merged per account rather than collapsed. `unavailable` ≠ `0`; `partial` and `rate_limited` are their own states. Advisor Do now/Next/Later from served urgency — no urgency means Later, never promoted. Default-off cards need all five fields or are not rendered. A file scan asserts no Meta lane word, owner, due date, `pause_ad`, repair-gap or Launchpad reaches a Google surface |
| 20 | **complete** | `23128cb62` | `lib/zero-base/google/manual-plan.ts`, `components/zero-base/google/{plan-view,plan-client}.tsx`, `app/c/[businessId]/google/plan/page.tsx` + 2 test files | 63 tests (40 module · 23 view) · full suite 8558/0 | copy, CSV, deep links and batch validation driven with real user events | `git revert 23128cb62`; canonical route only | Manual path primary and first. Order from served rank; unranked follow rather than being re-sorted. CSV is RFC 4180 + CRLF, with a test that the field count holds when a rationale contains a comma. Deep links always carry the account id. **"Reconciliation" is a Meta word** and never appears in Google pending copy. **No `pause_ad` control — absent, not disabled**, proven by a file scan that also asserts no POST. Batch validates one type / one account / ≤250 and executes nothing. The partially-applied specimen renders only because `partially_applied` is a real `GoogleExecutionStatus` |
| 21 | **complete** | `e948bfdd7` | `lib/zero-base/analytics/analytics-contract.ts`, `components/zero-base/analytics/**` (2), 4 `app/c/[businessId]/analytics/*` routes + 2 test files | 31 tests (22 adapter against real payload fixtures · 9 mounted) · full suite 8645/0 | source panels, measured-zero vs unavailable, GEO disclosures asserted on the rendered surface | `git revert e948bfdd7`; additive routes only | Adapters written **after** inventorying the real handlers. `aiPageCount` is `Math.min(rowCount, 50)` in the route, so the 50-proxy disclosure shows whenever the number does and a value on the cap is flagged; `top3Priorities` is already sliced server-side and is never padded or re-sliced. One source down degrades rather than blanks. **Zero call sites to `/api/ai/insights/generate`**, scan-proven |
| 22 | **complete** | `a15dfc63c` | `lib/zero-base/reports/{report-catalog,builder-model}.ts`, `components/zero-base/reports/**` (2), 5 `app/c/[businessId]/reports/**` routes + 2 test files | 35 tests (23 catalog/builder/**both flag branches against the real share route** · 12 mounted) · full suite 8680/0 | keyboard move/resize/undo, per-widget error+retry, CSV guard driven with real user events | `git revert a15dfc63c`; the share flag is left at its default false | Catalog read from the vendored JSON: **exactly 5 + 4 = 9**, identity and order asserted; Search Console and Klaviyo stay separate; coming-soon is **disabled, never hidden**. Undo replays the exact prior state, because a widget clamped at the edge does not move back the way it moved in. **WP-03A flag-on fails closed with zero report lookups and zero access checks**; flag-off still looks the report up. Canonical UI has zero mint/share controls and shows the Appendix C prerequisite |
| 23 | **complete** | `91bf1fbd8` | `lib/zero-base/manage/manage-contract.ts`, `components/zero-base/manage/**` (2), 5 `app/c/[businessId]/manage/**` routes incl. callback + 2 test files | 24 tests (14 contract · 10 mounted) · full suite 8704/0 | per-provider states, ceremony outcomes and the economics table asserted on the rendered surface | `git revert 91bf1fbd8`; no server behaviour was changed | Provider health is per provider; an unmentioned provider is **unknown**, not disconnected. **A write is not a read-back**: reconnect and delete each issue an independent read, and deletion treats a later 404 as the only confirmation — it can never print "deleted" from a response. Economics divergence names exact sources, values and consumers; `recommendedMode` has no control in its section. **Zero call sites to `/api/billing`**, scan-proven; plan gates nothing |
| 24 | **complete** | `5328ac1b4` | `lib/zero-base/ops/{ops-routes,repair-ceremony}.ts`, `components/zero-base/ops/repair-panel.tsx`, `app/ops/layout.tsx` + 16 `app/ops/**` routes + 2 test files | 39 tests (14 matrix/gate/separation · 25 repair ceremony) · 6 Playwright at 1280/768/390 · full suite 8729/0 | Flow J incident path with all four steps at every Ops width; repair progress and outcome driven with real user events | `git revert 5328ac1b4`; `/admin` was never modified | **Logic reused, never duplicated**: every Ops page mounts the existing admin component, asserted by test, and none contains a `fetch` or `/api/admin` string of its own. Route matrix is data — 16 tuples resolving both ways, each with a real file on disk. Server-side gate uses the same two guards as the admin shell. **Buyer link count to Ops is 0**, scan-proven. **The repair read-back gap is preserved, not papered over**: the admin PATCH performs none, so no receipt is printed, the gap is stated, and a re-check is offered; a transport failure is ambiguous rather than refused |
| 25 | **complete** | `8723609dc` | `lib/zero-base/marketing/ledger-marketing.ts`, `scripts/zero-base/capture-marketing-snapshots.ts`, `app/marketing-ledger.css`, scope attributes on 3 roots covering all ten pages + 1 test file | 16 tests · 6 Playwright at 1440/390/320 · full suite 8745/0 | typography actually applied (body ≥15px, line height >1.4, bounded measure), visible focus, no 320 overflow, no product surface in the DOM | `git revert 8723609dc`, or by hand: delete the stylesheet and the three attributes | **Copy snapshot captured BEFORE any edit** (575 fragments, ten pages) and asserted equal after, with claim-bearing pages named separately. Every CSS rule asserted scoped to `[data-adc-marketing]`; only an allowed token subset declared. Both leak directions guarded. The root page's `if (!session) return;` **soft forward is permitted explicitly** — it is not a gate — while business and admin guards remain refused. EN/TR reported from what each file branches on |
| 26 | not started | — | — | — | — | — | outside Phase A |
| 27A | not started | — | — | — | — | — | outside Phase A |
| 27B | awaiting separate authority | — | — | — | — | — | deployment gated |

## Notes

- **A transition audit rejected `PHASE_E_COMPLETE` at `f27eb81ee`**: the focused tests exercised isolated invented fixtures rather than the shipped clients against real handlers, so four boundaries that could never work still shipped — analytics/SEO clients guessing collection keys the endpoints never return, report duplicate/save/view sending or expecting shapes the routes do not use, a Manage reconnect POST to a route with no POST plus a cost-model wrapper misread and a deletion read-back against a route with no GET, and a Flow J repair panel referenced only by its own test. Corrected by `bf71a2999`, `993e0b089`, `bf6f78c7b` and `13b4274b0`, with 87 tests rebound to the actual route modules and 11 explicit regressions that fail against the old code. Detail: `PHASE_E_IMPLEMENTATION_REPORT.md` §10.
- **Phase E is complete at `13b4274b0`** (was `8723609dc` before the audit corrections), from the accepted Phase D head `1d769b534`. WP-21 through WP-25 add 33 canonical buyer routes plus the 16-leaf Ops product and Ledger presentation across the ten public pages. An earlier checkpoint reported `PHASE_E_BLOCKED` with WP-24/25 unstarted on grounds of capacity; that was honest about where the work stood but capacity is not a technical blocker, and both are now built and proven. `git diff 1d769b534..8723609dc` touches no resolver or decision-output file, so no ADR was required. The credential-free smoke shows the same three pre-existing failures as the Phase D baseline (3 failed in both; the passing count rose 83 → 95 from new Phase E coverage). Full evidence: `PHASE_E_IMPLEMENTATION_REPORT.md`.

- **Phase D acceptance review rejected the `PHASE_D_COMPLETE` claim at `23128cb62`.** Four blockers were found — an unmounted public share composition, unreachable brief/share creation, Launchpad claiming workflows that did not exist while sending bodies the handlers refuse, and Google clients typed against payloads the endpoints never return (including a real `.replace()`-on-undefined crash). All four are now corrected: `81a8526bf` (Blockers 3 and 4), `8f19290b0` (Blocker 2) and `173280181` (Blocker 1 — the public creative share now mounts on the real route, sanitized, with rotation and dead-state indistinguishability proven against the real store by a new `public-share` DB seam). The self-authored tests missed all four because they instantiated invented view types rather than comparing bodies and shapes to real routes. Full detail: `PHASE_D_IMPLEMENTATION_REPORT.md` §0 and §10.
- Phase D (WP-16 – WP-20) was built at `23128cb62`, from the accepted Phase C head `a09e6addb`. Fifteen canonical routes were added across five reviewable commits. `git diff a09e6addb..23128cb62` touches **no** resolver or decision-output file, so no ADR was required; no row-level `brief_variation` was introduced and no UI computes `buyerAction`. The local smoke shows the same three pre-existing failures as the Phase C baseline (71 passed / 3 failed in both). Full evidence: `PHASE_D_IMPLEMENTATION_REPORT.md`.

- WP-00 (`b082885be`), WP-00.5 (`59fd7118b`), WP-01 (`b36079f65`), WP-02 (`29715accb`), WP-03 (`e2c2d0885`) and WP-03A are committed; see the rows above for evidence. No dependency, provider, deployment or production state changed; `package-lock.json` is unmodified.
- WP-02's test is not self-referential. Every set is compared against the vendored JSON read fresh from disk, and that JSON must first hash to the digest recorded independently in `SOURCE.md` at vendor time; the scalar counts are master-plan literals. Both failure modes were exercised, not assumed.
- Reconciliation proven in code, not asserted in prose: 67 legacy records = 20 alias + 47 changed over 46 unique changed paths, because `/settings` is the only old path that splits (into `L-ME-ACCOUNT` and `L-C-M-BIZ`). 66 mapped + 9 retired + 1 dev-excluded = 76 legacy pages. 11 `new-surface` leaves, 10 of which have no legacy record — `L-ME-ACCOUNT` is the eleventh. The single range alias `gated:META-WF-02..08 menu` expands to exactly 7 workflow capabilities and any other `..` token is rejected.
- The planning worktree `/Users/harmelek/Adsecute` (`c46d91c2a`, 346 dirty files) was read only and remains untouched.
- WP-03 changed no observable API behaviour. `requireBusinessAccess` keeps its signature, status codes and message strings; a parity test pins all seven denial branches and asserts no membership read occurs for a caller who has not proven identity. The pre-existing migration guard in `lib/access.test.ts` passes untouched.
- WP-03A is **built and tested but never enabled.** `ZERO_BASE_REPORT_SHARE_FAIL_CLOSED` defaults to false, appears in no environment file, and is set only per-test with a restore. Flag-on reads nothing: the mint endpoint returns an identical body for existing and non-existent reports, and the public page's guard runs before the token promise is awaited, so no timing difference distinguishes a live token from a fabricated one. No stored snapshot is read, written or deleted in either branch.
- Appendix C authority for a deployed flag-on state: **absent.** Not requested and not required for local implementation.
- Phase B (WP-04 – WP-10) ran from `33df99879` and is **complete**. WP-09 and WP-10 were partial at `66dbfc3fb` and were finished by `45276d383` and `45a9ff1ab`; the partial commits are retained and are not relabelled.
- **Correction after acceptance review:** `45a9ff1ab` implemented the Agency directory as an unbounded server read plus client-side slicing, while this ledger claimed server pagination. That claim was wrong. `601094543` implements genuine SQL keyset pagination and adds a real-PostgreSQL seam over 121 clients, and `0a50fa6a8` replaces the layout's materialise-to-count gate with a bounded count query. The earlier commits are retained in history rather than rewritten.
- **Second correction after acceptance review:** review found two further cursor defects in `601094543` — a single mutable `pageCursor` overwrote every served row's return provenance when a later page loaded, and a malformed direct-page cursor was silently reinterpreted as page one *and* propagated back into every row's return link. `aaadbdd58` fixes both: rows are held as immutable page segments, and the direct page validates before reading and renders a recoverable state with a clean recovery URL. Both fixes are proven by reinstating the defect and watching the new tests fail. Full evidence: `PHASE_B_IMPLEMENTATION_REPORT.md`.
- Full stop evidence: `PHASE_A_IMPLEMENTATION_REPORT.md` in this directory.
- Phase C (WP-11 – WP-15) ran from `da7eb3298` and is **complete** at `55b442178`. Before it began, `6fef159b5` corrected a stale WP-10 suite figure in this ledger: the row said `7984/0` while `PHASE_B_IMPLEMENTATION_REPORT.md` said `8008/0`; 8008/0 is the measured value. The commit chain through `aaadbdd58` is unchanged and both Phase B acceptance-review corrections remain stated in full above.
- **A second Phase C acceptance review rejected the `PHASE_C_COMPLETE` claim again**, on a WP-14 production-contract defect: a nonexistent `apply-bid` path and a generic two-field dispatch body that every real handler refuses, both hidden by a mocked dispatch function. Corrected by `7889d4812`, which replaces the mock with 38 integration tests against the actual route modules.
- **Phase C acceptance review (2026-08-11) rejected the first `PHASE_C_COMPLETE` claim** and found WP-13, WP-14 and WP-15 materially incomplete despite green unit tests. All verified defects are named in the rows above and corrected by `ada7a6bf4`, `5bbfc2ca7` and `8c9e13eb8`. The original commits are retained and are not relabelled or rewritten.
- `git diff aaadbdd58..HEAD` still touches **no** resolver or decision-output file: nothing under `lib/creative-decision-engine/`, `lib/creative-decision-center/` or `lib/archive/`, and no `lib/meta/recommendations*`. The seven pre-existing files Phase C modified are the workflow overlay store/route, the preflight route, `lib/migrations.ts`, and two seam scripts. Full evidence: `PHASE_C_IMPLEMENTATION_REPORT.md`.
- **A second Phase E transition audit rejected the `PHASE_E_COMPLETE` claim again, at `07935bad3`**, naming five defect groups. All five are corrected at `d25fbb18a`. The audit's central finding is recorded plainly because it is the same failure mode as Phase C and D: **`lib/zero-base/reports/report-documents.test.ts` invented a `dataSource` field on `RenderedReportWidget` and the adapter read it, so the test passed while production resolved every widget to `""`.** The renderer reads `dataSource` off the widget *definition* and never emits it. Corrections: `0827fdee0` (analytics source state now comes from `/api/integrations/status`, keyed off the producer's own type, instead of a `sources` object only GEO emits — healthy GA4 and Search Console reads had been rendering as "not reported"); `04f6b3cb0` (the rendered payload is passed through whole and rendered by widget type, the builder no longer opens stored reports blank, and saving no longer destroys `metricKey`/`yMetrics`/`breakdown`/`accountId`/`limit`/`columns`/`tableDimension`/`axisMode`/`text`/`subtitle` — the round-trip test also caught `text` and `section` widgets being deleted on save for having no `dataSource`); `338117398` (Flow J was mounted with no `businessId`, so every click on the shipped repair button was a 400 — it now selects a workspace from `GET /api/admin/businesses`, confirms the provider action in scope, and stamps a re-read only after a readable health body); `d25fbb18a` (the WP-23 scope the master plan required: a real end-to-end OAuth return path for every offered provider, the Team scope on the actual routes, account assignment reusing the proven provider-assignment module, and Business name/currency settings). 48 tests across the four groups fail against `07935bad3`, verified by checking out the prior sources and re-running. Full evidence: `PHASE_E_IMPLEMENTATION_REPORT.md` §11.
- **A third Phase E audit rejected the `PHASE_E_COMPLETE` claim at `4a51ed4ee`**, naming four mounted production paths that were unreachable or dishonest. All four are closed at `4077db0c9`. One finding corrects **this documentation**, not just code: the Phase E report had declared GA4 property and Search Console site selection absent for want of a discovery route, when `/api/google-analytics/properties`, `/api/google-analytics/select-property`, `/api/google-search-console/sites`, `/api/google-search-console/select-site` and the GA4 picker helpers were all already on disk — the scope had simply never been surfaced, and the report asserted a limitation instead of checking. Corrections: `32636db96` (Flow H had no entry point at all — Integrations offered Reconnect for `needs_reconnect` and a dash for `not_connected`, so a never-connected provider could not be connected; Connect now uses the same OAuth start and sanitized `returnTo`, and support is computed from whether a real start route exists); `03771fc44` (GA4/Search Console selection reusing the proven helpers, confirming from an independent re-read — GA4 discovery reports `selectedPropertyId`, while the sites route reports no selection at all so Search Console re-reads the integration record's `provider_account_id`); `b07309539` (the Ops workspace selector asked for `?limit=200` but `/api/admin/businesses` hardcodes `limit = 30`, so the 31st workspace was unselectable and the short list looked complete — pages are now walked by the route's own `total`/`limit`, the health read requires the echoed `businessId` to match the selected workspace, and the confirmation renders the state that GET actually returned instead of pointing at a legacy board it never updated); `4077db0c9` (CSV export was an enabled button wired to nothing in front of a working endpoint — it now calls the real export route with the clicked `widgetId`, triggers a genuine download, and reports a refusal instead of failing silently). 28 tests across the four groups fail against `4a51ed4ee`. Full evidence, including the exact gate commands: `PHASE_E_IMPLEMENTATION_REPORT.md` §12.
- **A fourth Phase E audit rejected the `PHASE_E_COMPLETE` claim at `6467a7d80`** for three contract defects, after independently passing 121 targeted tests, typecheck and lint. All three are closed at `c80fb9791`, and all three shared one shape: the UI claimed more than the handlers support. (1) Every OAuth start route for Meta/Google/GA4/Search Console requires `minRole: "collaborator"`, but guests were shown enabled Connect and Reconnect controls and navigated into a JSON 403; both controls now mirror that gate, and a guest gets a stated read-only reason. (2) Shopify was listed in `OAUTH_START_PROVIDERS` as a start that "can actually complete" — invoking the real handler shows a request with no `shop` is redirected to `/shopify/connect` with `businessId` and `returnTo` dropped, so the generic Connect could never complete. Shopify now has its own honest entry linking to the real setup surface and stating that authorization starts in Shopify and cannot be confirmed here; a direct reauthorization is offered **only** when the stored integration supplies an authoritative `myshopify.com` domain, which the handler does complete (verified by invoking it, not by reading source). (3) `readHealthBody({businessId}, businessId)` returned a non-null reading and a test encoded that as valid, so a 200 carrying no health was stamped as a successful re-read; a health claim now requires a `status` core with valid `state`/`connected`, and a present-but-malformed optional field makes the reading unknown rather than being downgraded to "not reported". 18 tests fail against `6467a7d80`. Honest residual: Shopify cannot support a first-time in-product round trip at all, and the surface now says so rather than implying otherwise. Full evidence and exact gate commands: `PHASE_E_IMPLEMENTATION_REPORT.md` §13.
- **A fifth Phase E acceptance at `ed9e33179` found exactly one remaining defect**, corrected at `600130a2b`. `IntegrationsView` evaluated the Shopify branch **before** the role check, so the §13.1 posture never reached Shopify and a guest still received an enabled action link in both the external-install and the verified-domain reauthorize states. Not cosmetic: Shopify's start route is session-authenticated only, but `app/api/oauth/shopify/callback/route.ts` and `app/api/oauth/shopify/finalize/route.ts` both require `minRole: "collaborator"`, so a guest who followed the link was sent through a long external install and refused at the end of it. The role check now runs first for every provider — a guest gets the same stated read-only reason and no Shopify anchor at all, so no navigation can start — while admin and collaborator keep the honest external-install and verified-domain reauthorize semantics unchanged. A follow-on typecheck catch bound the handler-existence assertion to `finalize`'s real verb (POST only). Three guest regressions fail against `ed9e33179`; the permitted-role tests pass in both. This also supersedes §13.5's claim that no collaborator gate applied to Shopify: true of the start route, misleading about the flow. Full evidence and exact gate commands: `PHASE_E_IMPLEMENTATION_REPORT.md` §14.
