/**
 * DOM matchers for component tests.
 *
 * Loaded for every test file; `@testing-library/jest-dom` only augments
 * `expect`, so node-environment tests that never touch the DOM are unaffected.
 */
import "@testing-library/jest-dom/vitest";
